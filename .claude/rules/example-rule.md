---
paths:
  - "src/**/*.ts"
---

# Beispiel-Regel

Diese Regel wird nur geladen, wenn Dateien unter `src/**/*.ts` bearbeitet werden.
Beschreibe hier themenspezifische Vorgaben (z. B. Fehlerbehandlung, Namenskonventionen).
